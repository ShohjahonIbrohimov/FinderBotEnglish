import React from "react";
import Button from "../../Reusable/Button"

const index = () => {
  return (
    <div>
      <span className="page_section_title">Call center</span>
      <Button name="Опубликовать" />
    </div>
  );
};

export default index;
