import React from "react";
import Button from "../../Reusable/Button";

const index = () => {
  return (
    <div>
      <span className="page_section_title">Main</span>
      <button type="submit" className="btn-main btn-main--dark mr-8">
        Add
      </button>
    </div>
  );
};

export default index;
